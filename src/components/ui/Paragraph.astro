---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---


<p class={`mt-8 text-muted text-lg max-w-xl leading-relaxed text-pretty ${className}`}>
  <slot />
</p>
