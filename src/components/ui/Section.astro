---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<section class={`relative min-h-screen flex items-center py-20 md:py-28 overflow-hidden ${className}`}>
  <slot />
</section>
