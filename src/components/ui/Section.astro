---
interface Props {
  class?: string;
  id?: string;
}

const { class: className = '', id } = Astro.props;
---

<section id={id} class={`mx-auto px-1 sm:px-4 md:px-6 py-20 md:py-28 overflow-hidden ${className}`}>
  <slot />
</section>
